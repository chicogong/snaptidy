# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.venv/
venv/
env/

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# -----------------------------------------------------------
# SnapTidy generated output files
# -----------------------------------------------------------

# Default output directory (scan index, duplicates, plans, reports)
snaptidy_output/

# SQLite databases (scan index, dedup cache)
*.db

# CSV data files (duplicates, move plans, logs)
*.csv

# HTML reports (album report, thumbnail preview)
*.html

# JSON manifests and checkpoints
photo_index.json
import_checkpoint.json
import_report.json
plan_manifest.json
quick_*.json
privacy_report.json

# AppleScript fallback files
*.applescript

# Undo records
undo_records/

# Review folders (moved duplicates)
review/

# Test data generated artifacts (keep scripts, ignore outputs)
test_data/*.db
test_data/*.csv
test_data/*.html
test_data/*.json
test_data/*.txt
test_data/*.applescript
test_data/undo_records/
test_data/review/
test_data/venv/
